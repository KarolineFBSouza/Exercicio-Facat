function Impares() {
    let contadora = 1;
    wile(contadora < 21); {
        if (contadora % 2 != 0) {
            console.log (contadora + "Esse número é impar!")
        }
        contadora ++
    }
}