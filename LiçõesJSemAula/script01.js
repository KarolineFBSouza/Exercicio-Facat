function exemploSWhile() {
    //let numero = parseInt(prompt("Digite um Número: "))
    let contadora = 1;
    while (contadora < 10) {
        console.log(contadora)
        contadora++
    }
}